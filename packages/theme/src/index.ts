/**
 * @revdev/theme — Shared terminal theme tokens.
 *
 * Provides consistent color palette and theme tokens used by both
 * Studio's terminal pane (xterm.js) and the standalone Terminal TUI (Go).
 *
 * @packageDocumentation
 */

export { COLORS, type ColorPalette } from './colors.js';
export { TERMINAL_THEME, type TerminalTheme } from './terminal.js';
